# Ensure optional dependencies are installed
optional=true
# Allow npm to be more lenient with optional dependencies
legacy-peer-deps=true
# Use npm install instead of npm ci for Heroku (temporary workaround)
package-lock=false
package-lock=false
